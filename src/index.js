import { createElement } from './createElement'
import { createVirtualElement } from './createVirtualElement'
import { updateDOM } from './updateDOM'

const NARC = { createElement, createVirtualElement, updateDOM }

export default NARC
